apiVersion: v1
kind: Service
metadata:
  name: back-ventas
  namespace: innovatech
  labels:
    app: back-ventas
    tier: backend
spec:
  selector:
    app: back-ventas
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP

